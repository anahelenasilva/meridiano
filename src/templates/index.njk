<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Meridiano Briefings</title>
    <link rel="stylesheet" href="/static/style.css">
</head>
<body>
    <div class="container">
        <div class="navigation">
             <a href="/articles">View Stored Articles »</a>
        </div>

        <h1>Generated Briefings</h1>

        <div class="profile-filter">
            Filter by Profile:
            <a href="/" class="profile-link {{ 'active' if not current_feed_profile else '' }}">All</a>
            {% for profile in available_profiles %}
                <a href="/?feed_profile={{ profile }}" class="profile-link {{ 'active' if current_feed_profile == profile else '' }}">
                    {{ profile }}
                </a>
            {% endfor %}
        </div>

        {% if briefs %}
            <ul class="brief-list">
                {% for brief in briefs %}
                <li class="brief-item">
                    <a href="/brief/{{ brief.id }}" class="brief-link">
                        Briefing from {{ brief.generated_at | dateformat('DD/MM/yyyy HH:mm') }}
                    </a>
                    <span class="profile-badge list-badge">{{ brief.feed_profile }}</span>
                    <span class="brief-meta"> (ID: {{ brief.id }})</span>
                </li>
                {% endfor %}
            </ul>
        {% else %}
            <p>No briefings have been generated yet. Run the briefing system to generate new briefings.</p>
        {% endif %}

    </div>
</body>
</html>
